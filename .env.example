# ============================================================================
# DeepMind Workspace — Environment Configuration
# Copy to .env and fill in your actual values
#
# IMPORTANT: Generate secure keys FIRST:
#   python scripts/generate_secrets.py --env 2>> .env
#
# For full secrets management documentation, see docs/SECRETS.md
# ============================================================================

# ============================================================================
# CRITICAL SECRETS — Managed by secrets provider (see config/app.yaml -> secrets:)
# Generate with: python scripts/generate_secrets.py
# ============================================================================
APP_SECRET_KEY=
JWT_SECRET_KEY=
JWT_REFRESH_SECRET_KEY=

# ============================================================================
# Secrets Provider Configuration
# Options: environment (default) | vault | aws | local_encrypted
# ============================================================================
SECRETS_PROVIDER=environment

# HashiCorp Vault (when SECRETS_PROVIDER=vault)
# VAULT_ADDR=https://vault.example.com:8200
# VAULT_TOKEN=

# AWS Secrets Manager (when SECRETS_PROVIDER=aws)
# AWS_REGION=us-east-1
# AWS_SECRET_NAME=deepmind/secrets
# AWS credentials via standard AWS env vars (AWS_ACCESS_KEY_ID, etc.)

# ============================================================================
# API Keys — Managed by secrets provider - see docs/SECRETS.md
# Set here for environment provider, or store in Vault/AWS/encrypted file
# ============================================================================

# DeepSeek API
DEEPSEEK_API_KEY=
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
# DEEPSEEK_MODEL=deepseek-chat
# DEEPSEEK_REASONING_MODEL=deepseek-reasoner

# OpenAI API (for GPT-4o model option)
OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o

# Together AI (for FLUX.1 image generation)
TOGETHER_API_KEY=
# TOGETHER_BASE_URL=https://api.together.xyz/v1

# ============================================================================
# Application
# ============================================================================
APP_HOST=0.0.0.0
APP_PORT=8080
APP_ENV=production
APP_LOG_LEVEL=INFO

# ============================================================================
# Database Paths
# ============================================================================
# For local development:
# SQLITE_DB_PATH=./data/conversations.db
# CHROMADB_PATH=./data/chromadb
# For Render (with persistent disk at /data):
SQLITE_DB_PATH=/data/conversations.db
CHROMADB_PATH=/data/chromadb

# ============================================================================
# Connector Tokens — Managed by secrets provider - see docs/SECRETS.md
# ============================================================================

# GitHub Integration
GITHUB_TOKEN=
# GITHUB_DEFAULT_ORG=

# Dropbox Integration (disabled by default)
# DROPBOX_APP_KEY=
# DROPBOX_APP_SECRET=
# DROPBOX_REFRESH_TOKEN=

# Google Drive Integration (disabled by default)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=http://localhost:8080/api/connectors/google/callback

# ============================================================================
# Embedding / Context (defaults in config/app.yaml — override here if needed)
# ============================================================================
# EMBEDDING_MODEL=all-MiniLM-L6-v2
# EMBEDDING_DIMENSION=384
# MAX_CONTEXT_TOKENS=128000
# SUMMARY_TRIGGER_TOKENS=96000
# RECENT_MESSAGES_KEEP=20
